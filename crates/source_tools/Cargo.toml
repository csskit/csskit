[package]
name = "source_tools"
version.workspace = true
authors.workspace = true
description = "Shared source locations and generic token cursors."
edition.workspace = true
homepage.workspace = true
keywords.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[lib]
bench = false

[dependencies]
allocator-api2 = { workspace = true }
csskit_arena = { workspace = true, optional = true }
miette = { workspace = true, optional = true }
serde = { workspace = true, features = ["derive"], optional = true }

[features]
default = []
miette = ["dep:miette"]
# The `ToSpan` impls for the `csskit_arena` collections.
csskit_arena = ["dep:csskit_arena", "csskit_arena/collections"]
serde = ["dep:serde"]
